services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: noodle
      POSTGRES_PASSWORD: noodle
      POSTGRES_DB: noodle_ordering
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: